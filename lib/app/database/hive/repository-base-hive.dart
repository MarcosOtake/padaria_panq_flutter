/* import 'package:hive/hive.dart';

class BaseBoxHive{

    ///Propertiers
    final Box _box;

    ///Getters Setters
    Box get box{
        return _box;
    }

    ///Construtor
    BaseBoxHive(this._box);

    ///
    bool get boxIsClosed => !(this._box?.isOpen ?? false);

} */
